remove .git from the subfolder.

in main directory,
1. set master & check branch & commits

    git push --set-upstream origin master

    git branch -vv

2. Remove the Folder from Git's Cache//

    git rm -r --cached folder-name

3. add, commit, push

